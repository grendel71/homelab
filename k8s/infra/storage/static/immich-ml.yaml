apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: immich-ml-pvc
  namespace: immich
spec:
  storageClassName: replicated-x3-block-store
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
